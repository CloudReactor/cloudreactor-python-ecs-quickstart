# Override this file with steps to perform after the Docker
# image has been uploaded, but before the task has been created
# in ECS or CloudReactor.

- debug:
    msg: "Post-build: nothing to do"
    verbosity: 1
